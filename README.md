# Repo-Health-Tool
Two zero-dependency GitHub Actions for repo hygiene: a docs-drift checker that flags JSDoc/README comments out of sync with the code, and a dependency-health dashboard that flags outdated, deprecated, abandoned, or vulnerable npm packages. Ships as CLIs too — no dependencies to install.
